// Test skipped: Anthropic provider was removed (paid). Keeping file for reference.
import { describe, it } from "vitest";

describe.skip("Anthropic auth token", () => {
	it("placeholder", () => {});
});
